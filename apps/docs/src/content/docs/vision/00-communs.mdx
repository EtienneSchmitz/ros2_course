---
title: "Socle commun aux ateliers vision"
description: Mise en place partagée par les quatre ateliers à la carte — dépendances, monde Gazebo, cv_bridge, squelette du nœud detector et back-projection vers /detections.
sidebar:
  label: "Socle commun"
  order: 2
---

import { Aside, Tabs, TabItem } from "@astrojs/starlight/components";

<Aside type="note" title="À lire avant n'importe quel atelier">
Les quatre ateliers du jour ([Formes](/vision/01-formes-opencv/),
[ArUco](/vision/02-aruco/), [YOLO](/vision/03-yolo/),
[Chiffres](/vision/04-chiffres/)) sont **à la carte** : vous en choisissez **un** selon
votre envie. Cette page rassemble ce dont **tous** ont besoin — faites-la une fois, puis
filez vers l'atelier de votre choix.
</Aside>

Tous les ateliers suivent la même trajectoire **hybride** : on apprend la technique en
**OpenCV/Python pur**, puis on l'emballe dans un **nœud ROS 2** qui publie un
`Detection.msg` sur `/detections`. C'est la brique « perception » du fil rouge : votre
détecteur remplacera le `detector` factice du Jour 1.

## 1. Dépendances vision

La **stack image ROS** (`cv-bridge`, `image-transport`, `image-publisher`) et
**`opencv-contrib-python`** (qui fournit le module `aruco`) sont installées à l'étape
[Installation](/installation/01-ubuntu/#ros-2-kilted--gazebo-ionic) — vérifiez-les avant
de commencer :

```bash
python3 -c "import cv2, cv_bridge; print(cv2.__version__)"
python3 -c "import cv2.aruco; print('aruco ok')"
```

<Aside type="caution" title="Un seul paquet OpenCV à la fois">
N'installez **pas** `opencv-python` en plus de `opencv-contrib-python` : les deux
entrent en conflit. En cas de doute : `pip uninstall opencv-python`.
</Aside>

<Aside type="note" title="Dépendances IA (YOLO / Chiffres seulement)">
Les ateliers [YOLO](/vision/03-yolo/) et [Chiffres](/vision/04-chiffres/) ajoutent
`torch` / `torchvision` / `ultralytics` (via pip). L'installation est décrite **dans ces
ateliers** : inutile si vous choisissez Formes ou ArUco.
</Aside>

## 2. Lancer le monde « vision »

La caméra et les objets vivent dans le package **`bootcamp_vision`** (monde
`vision_table.sdf` : une caméra fixe en **vue de dessus** observe une table).

<Tabs>
<TabItem label="Terminal 1 — monde + caméra">

```bash
ros2 launch bootcamp_vision vision_world.launch.py
```

Vérifiez le flux dans un autre terminal :

```bash
ros2 topic list | grep camera     # /camera/image_raw + /camera/camera_info
ros2 topic hz /camera/image_raw   # ~24 Hz
```

</TabItem>
<TabItem label="Terminal 2 — faire apparaître un objet">

Chaque atelier a son preset (`shapes`, `aruco`, `yolo`, `digit`) :

```bash
ros2 launch bootcamp_vision spawn_object.launch.py object:=shapes
# object:=aruco aruco_id:=1   |   object:=yolo   |   object:=digit
```

</TabItem>
</Tabs>

<Aside type="tip" title="Visualiser sans coder">
```bash
ros2 run rqt_image_view rqt_image_view /camera/image_raw
```
Gardez cette fenêtre ouverte : vous y verrez ce que « voit » votre nœud.
</Aside>

## 3. De l'image au `/detections`

Le cœur ROS 2, **commun à tous les ateliers** : on reçoit l'image, on détecte, on publie.
Quatre briques.

### 3.1 `cv_bridge` — du message ROS au tableau OpenCV

`cv_bridge` convertit un `sensor_msgs/Image` en image OpenCV (tableau NumPy `H × W × C`,
en **BGR**) :

```python
from cv_bridge import CvBridge
frame = CvBridge().imgmsg_to_cv2(msg, "bgr8")   # -> np.ndarray BGR
```

### 3.2 Le message `Detection` (fourni par `bootcamp_vision`)

`bootcamp_vision` fournit deux types de **référence** prêts à l'emploi :

```python
from bootcamp_vision.msg import Detection, DetectionArray
```

| Champ de `Detection` | Sens |
| --- | --- |
| `string class_name` | nom de classe (ex. `"coffee_pack_1"`) |
| `int32 class_id` | identifiant numérique de classe |
| `float32 score` | confiance `[0..1]` |
| `float32 u, v, w, h` | boîte 2D dans l'image (pixels) |
| `geometry_msgs/Point position` | position sur la table (m), `z = 0.40` |

`DetectionArray` = `std_msgs/Header header` + `Detection[] detections` : **une liste par
image**. On reprend le `header` de l'image (stamp + `frame_id`) pour rester synchronisé
avec la TF.

<Aside type="note" title="Et votre package d'interfaces du Jour 1 ?">
Pour le fil rouge (Jour 5), le projet utilise **votre** package d'interfaces
(placeholder des énoncés : `mon_projet_interfaces`). En attendant, importez directement
les types de `bootcamp_vision` ci-dessus, ou recopiez `msg/Detection.msg` dans votre
package et changez l'import — les champs sont identiques.
</Aside>

### 3.3 Estimer la position — back-projection

Avec une caméra **en vue de dessus** et les intrinsèques connues, on remonte d'un pixel
`(u, v)` vers un point sur le plan de la table (`z = 0.40`). La distance caméra→table
(`1.00 m`) est documentée dans le **README de `bootcamp_vision`**.

```python
FX, FY, CX, CY = 935.485, 935.485, 640.0, 360.0   # cf. /camera/camera_info
CAM_HEIGHT = 1.00          # caméra z=1.40, plateau z=0.40 -> 1.00 m
TABLE_Z = 0.40

def backproject(u, v):
    """Pixel (u,v) -> position (x, y, z) sur le plateau."""
    x = (u - CX) / FX * CAM_HEIGHT
    y = (v - CY) / FY * CAM_HEIGHT
    return x, y, TABLE_Z
```

<Aside type="caution" title="Position seule, pas d'orientation">
Le message ne porte qu'une **position** (`Point`) : on suppose l'objet **posé à plat** et
on n'estime pas son orientation. Seul l'atelier [ArUco](/vision/02-aruco/) sait calculer
une **pose 6D** — mais il faut alors étendre le message (ou publier une TF) pour la
transporter. Vérifiez les **signes** des axes selon `camera_optical_frame` (REP-103).
</Aside>

### 3.4 Le squelette du nœud `detector`

Le **patron commun** : souscrit à la caméra, publie un `DetectionArray` sur
`/detections`. Chaque atelier ne remplit que le bloc « détection » dans `on_image`.

```python
import rclpy
from rclpy.node import Node
from sensor_msgs.msg import Image
from geometry_msgs.msg import Point
from cv_bridge import CvBridge
from bootcamp_vision.msg import Detection, DetectionArray

class Detector(Node):
    def __init__(self):
        super().__init__("detector")
        self.bridge = CvBridge()
        self.pub = self.create_publisher(DetectionArray, "/detections", 10)
        self.create_subscription(Image, "/camera/image_raw", self.on_image, 10)

    def on_image(self, msg):
        frame = self.bridge.imgmsg_to_cv2(msg, "bgr8")
        out = DetectionArray()
        out.header = msg.header          # stamp + frame_id (camera_optical_frame)
        # >>> bloc atelier : pour chaque objet détecté <<<
        #   out.detections.append(self.make_det("triangle", 0, 1.0, u, v, w, h))
        self.pub.publish(out)

    def make_det(self, class_name, class_id, score, u, v, w, h):
        det = Detection()
        det.class_name, det.class_id, det.score = class_name, int(class_id), float(score)
        det.u, det.v, det.w, det.h = float(u), float(v), float(w), float(h)
        x, y, z = backproject(u, v)
        det.position = Point(x=x, y=y, z=z)
        return det


def main():
    rclpy.init()
    rclpy.spin(Detector())
    rclpy.shutdown()
```

Pour tester, avec le monde lancé et un objet spawné :

```bash
ros2 run mon_projet_vision detector     # votre package
ros2 topic echo /detections             # un DetectionArray par image
```

## Choisir son atelier

<Aside type="tip" title="Quatre ateliers, un seul à choisir">
- 🟦 **[Formes (OpenCV)](/vision/01-formes-opencv/)** — vision classique : filtres,
  contours, couleur. Aucun entraînement.
- 🎯 **[ArUco (pose 6D)](/vision/02-aruco/)** — marqueurs fiduciaires, pose **6D**
  robuste. Le plus fiable.
- 🧠 **[YOLO (détection)](/vision/03-yolo/)** — détection par IA, modèle pré-entraîné
  puis **entraîné sur vos objets**.
- 🔢 **[Chiffres (CNN)](/vision/04-chiffres/)** — entraînez **vous-même** un réseau de
  neurones (MNIST).
</Aside>
