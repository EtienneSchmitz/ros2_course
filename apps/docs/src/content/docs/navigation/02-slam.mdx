---
title: 2. SLAM avec slam_toolbox
description: Cartographie en ligne avec slam_toolbox en mode online async, sauvegarde de carte.
sidebar:
  order: 3
---

import { Aside, Tabs, TabItem } from "@astrojs/starlight/components";

**SLAM** = *Simultaneous Localization And Mapping*. Le robot construit
une carte de son environnement **en même temps** qu'il s'y localise.

Sur ROS 2, l'implémentation de référence pour des bases 2D avec LiDAR
est [`slam_toolbox`](https://github.com/SteveMacenski/slam_toolbox).

## Modes de slam_toolbox

| Mode | Quand l'utiliser |
| --- | --- |
| **`online async`** | Le robot bouge ; SLAM tourne en temps quasi-réel, perd un peu de précision mais ne bloque pas le robot. **Mode du cours.** |
| `online sync` | Strict temps réel sans drop. Coûteux. |
| `offline` | Replay d'un bag enregistré, pour optimisation hors-ligne. |
| `lifelong` | Mise à jour continue d'une carte existante. |

## Entrées / sorties

```text
   /scan (LaserScan)   ───┐
   /tf (odom → base_link) ─┤
                           ▼
                    slam_toolbox  ──► /map (OccupancyGrid)
                                  ──► /tf (map → odom)
                                  ──► /map_metadata
```

Ce qu'il faut surtout retenir : `slam_toolbox` publie la transformation
**`map → odom`**, qui corrige la dérive de l'odométrie en utilisant les
features du scan laser.

## Lancement en parallèle de la sim

<Aside type="note" title="Hands-on (à venir)">
Quand le package `lekiwi_navigation` sera prêt, tu lances simultanément
la sim et le SLAM :

<Tabs>
<TabItem label="Terminal 1 — sim">

```bash
ros2 launch lekiwi_bringup sim_base.launch.py
```

</TabItem>
<TabItem label="Terminal 2 — SLAM">

```bash
ros2 launch lekiwi_navigation slam.launch.py
```

</TabItem>
<TabItem label="Terminal 3 — RViz">

```bash
rviz2 -d $(ros2 pkg prefix lekiwi_navigation)/share/lekiwi_navigation/rviz/slam.rviz
```

</TabItem>
</Tabs>

Dans RViz, tu dois voir :

- l'`OccupancyGrid` `/map` apparaître au fur et à mesure ;
- la silhouette du LeKiwi suivre la base ;
- les rayons laser en bleu.

</Aside>

## Téléop pour cartographier

Lance en plus la téléop clavier (cf. [chapitre précédent](/navigation/01-base-mobile/#téléop-clavier))
et balade le robot dans le monde. Quelques principes pour une bonne carte :

- **Vitesses modérées** (< 0.3 m/s) : les itérations SLAM ont le temps de
  fitter chaque scan.
- **Boucle fermée** : reviens régulièrement à un endroit déjà visité,
  ça aide l'algo à corriger la dérive (boucle de fermeture).
- **Couverture complète** : longe les murs pour qu'ils soient nets.

## Sauvegarder la carte

Une fois satisfait, sauvegarde la carte :

```bash
ros2 run nav2_map_server map_saver_cli -f ~/ros2_bootcamp_ws/maps/lekiwi_world
```

Cela produit deux fichiers :

- `lekiwi_world.yaml` : métadonnées (origine, résolution, seuils).
- `lekiwi_world.pgm` : image grayscale de l'OccupancyGrid.

Tu peux ensuite rejouer la carte sans SLAM (mode pur localisation, AMCL
ou slam_toolbox en `localization`).

## Prochaine étape

[Navigation autonome avec Nav2](/navigation/03-nav2/).
